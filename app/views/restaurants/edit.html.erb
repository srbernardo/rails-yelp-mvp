<div class="container">
  <h1>Editing restaurant</h1>

  <%= render "form", restaurant: @restaurant %>

  <%= link_to "Back to #{@restaurant.name}", @restaurant, class: "btn btn-secondary mt-2" %>
</div>
